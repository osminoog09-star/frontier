# FRONTIER handoff

Дата: 2026-06-05

## Текущее состояние

- Публичный сайт: https://osminoog09-star.github.io/frontier/
- Публичная игра: https://osminoog09-star.github.io/frontier/frontier.html
- Последний локальный срез: `v2.18 - Architect command panel`.
- Базовая игра: `frontier.html`, сборка через `frontier.template.html` + `_core.js`.
- Текущий `GAME_VERSION`: `2.18`. RimWorld-стройка: типы/качество/bedroll/материал-саман. В v2.18 Architect стал ближе к RimWorld-паттерну: слева категории, справа отдельная панель команд `ПРИКАЗЫ`; кнопки и build ids сохранены. Дальше: песчаник, материал->красота/качество глубже, температура; карта-мир Phase 3.5.
- Harness: Scenarios A-CQ (95) стабильно зелёные. Был флак в AC (RNG-ИИ) — починен пиннингом пешки.
  ПРАВИЛО: тесты детерминированные, прогонять `node _harness.js` несколько раз перед push.
- Дизайн-курс: **RimWorld-like фундамент + Frontier-идентичность**. Не копировать RimWorld как тему; брать понятные жанровые паттерны и наполнять их Диким Западом.

## QA для Claude после v2.15

- Пользовательский скрин `fix/Screenshot 2026-06-02 171333.png` проверен: на нём старая нижняя лента стройки расползалась по низу экрана и подходила к миникарте.
- В текущей v2.15 это состояние закрыто: стройка находится в левом `#architect`, в `#bottombar` остались только `Отмена` и `Снести`; старых build-кнопок внизу нет.
- Desktop layout smoke 1990x1200: `#architect`, `#objective-hud`, `#minimap-wrap`, `#bottombar`, `#sidebar` не пересекаются; runtime/page errors = 0.
- Public smoke GitHub Pages: `frontier.html` отдаёт `GAME_VERSION=2.15`, закрытые `.build-group-panel` имеют `display:none`, выбор фермы через Архитектор создаёт blueprint на canvas, runtime/page errors = 0.
- Локальная папка `fix/` намеренно не коммитится: это входящие скриншоты игрока. Временные smoke-скрины Codex удалены.
- Если следующий агент будет трогать UI стройки, обязательно проверить desktop 1600x900 и 1990x1200: группы Архитектора, миникарту, нижнюю панель, цель и правый sidebar.

## QA для Claude после v2.16

- Human audit 1600x900: сайт без horizontal overflow; меню игры открывает `Как играть`, `Роадмап / Обновления`, `Достижения`; новая игра запускается без console/page errors.
- В игре проверены вкладки sidebar (`Пешки`, `Расп.`, `Наука`, `Лог`), выбор пешки и priority-panel, все группы Архитектора, постановка фермы, speed/save/load.
- Визуальная правка: achievement toast смещён ниже `objective-hud`, чтобы toast «Первый дом» не перекрывал цель; стартовый help-popup больше не говорит старое «лагерь», теперь «спальный мешок».

## QA для Claude после v2.18

- По жалобе игрока на “стройку как в RimWorld” UI Архитектора больше не работает как длинный accordion внутри левой панели.
- Реализовано: слева стабильные категории `Архитектор`; при выборе категории справа открывается отдельная фиксированная панель `ПРИКАЗЫ`. Открыта ровно одна категория, активная команда держит свою категорию открытой.
- Важно для следующих агентов: `id` всех build-кнопок сохранены; gameplay-логика размещения не переносилась и не переименовывалась.
- Browser smoke 1280x720: новая игра, закрытие help-popup, все 8 категорий; панель команд справа от `#architect`, не пересекает `#objective-hud`, `#minimap-wrap`, `#sidebar`, `#bottombar`.
- Regression smoke: `Зоны -> Грядка (зона)` активируется, движение мыши по canvas не создаёт console/runtime errors. Harness A-CQ (95) green.

## QA для Claude после v2.17

- Пользовательский `frontier-log-day1.json` показал повторяющийся runtime error: `Cannot read properties of undefined (reading 'size')` в `render()`, stack `frontier.html:3652`.
- Причина: `G.buildMode='zone_grow'`/другие `zone_*` не существуют в `BUILDS`, но hover-preview читал `BUILDS[G.buildMode].size`. Это ломало frame loop и визуально давало чёрные провалы на карте.
- Исправлено: preview зон рисуется отдельно через `ZONE_DEFS`, неизвестный `buildMode` не падает, `normalizeGameState()` фильтрует неизвестные building types, render/click/tooltip/demolish guard-ят `BUILDS[b.type]`.
- Проверка: Scenario CQ зелёный; browser smoke 2048x1280 — `Зоны -> Грядка (зона)`, движение мыши по canvas, runtime/page/console errors = 0.

## Что изменилось в v2.04

- POI стали игровыми событиями: `explorePoi()` одноразово выдаёт результат и ставит `searched/exploredDay`.
- Руины дают еду/медикаменты/науку, золотая жила — золото/руду, лагерь бандитов — добычу и малый налёт.
- Карточка POI показывает исход или статус «уже обследовано»; save/load сохраняет состояние. Scenario CG. Harness A-CG (84) green.

## Что изменилось в v2.01

- Добавлен слой `G.pois`: руины поселенцев, лагеря бандитов и золотые жилы.
- POI не ставятся на воду/скалы, не появляются рядом со стартом, не перекрываются деревьями/камнями и сохраняются в save/load.
- Клик по POI показывает карточку, tooltip показывает название. Scenario CE. Harness A-CE (83) green.

## Что изменилось в v1.96

- Карта делится на крупные биом-регионы: пустыня, равнина, прерия, лес и горы.
- Добавлены чистые хелперы `biomeAt(e,m)` и `biomeTerrain(biome)`; `tile.biome` сохраняется для будущей плотности деревьев/камней/животных.
- Рендер и save/load карты не сломаны; Scenario BZ добавлен, harness A-BZ (78) green.

## Что изменилось в v1.70

- Строитель теперь резервирует ресурсы, несёт видимый `buildpack`/📦 к чертежу и только после доставки начинает progress.
- Info-overlay чертежа показывает `Материалы: в пути`.
- `buildpack` защищён от обычной перевозки на склад.
- Scenario BB обновлён, Scenario BD добавлен; `frontier.html` собран через `node build.js`.

## Что изменилось в v1.69

- Закрытая комната считается крытой: пешка внутри не получает `Мокнет` под дождём/грозой.
- Пешка снаружи продолжает мокнуть, так что укрытие комнаты имеет видимый смысл.
- Room-панель показывает строку крыши, а overlay мебели добавляет `крыша есть` в label комнаты.
- Scenario BC добавлен; `frontier.html` собран через `node build.js`.

## Что изменилось в v1.68

- Чертежи зданий ставятся как план без мгновенного списания ресурсов.
- Строитель списывает материалы при первом начале работы; если ресурсов не хватает, progress не растёт.
- Info-overlay показывает `Материалы: готовы` или `Ждёт материалы: wood 0/5`.
- Диагностика застрявших чертежей теперь пишет про нехватку материалов, а не только про строителей.
- Scenario BB добавлен; `frontier.html` собран через `node build.js`.

## Что изменилось в v1.67

- Проведён desktop browser smoke Architect-панели: нет пересечения с целью, миникартой и нижней панелью.
- Стартовый help-popup и меню «Как играть» синхронизированы с левым «Архитектором», старый текст про выбор постройки снизу убран.
- `info-overlay` на desktop сдвинут правее/ниже, чтобы не прятаться под Architect-панелью.
- Gameplay не менялся; после `_core.js` правки запускать `node build.js` обязательно выполнено.

## Что изменилось в docs/site sync 2026-06-03

- `ROADMAP.md` переписан как понятный план: RimWorld-фундамент, Frontier-идентичность, качество, сценарии, онлайн позже.
- `index.html` обновлён до v1.68/A-BB и показывает актуальный badge про 54 harness-сценария.
- `docs/TECH_ROADMAP.md` и `docs/DESIGN.md` синхронизированы с направлением v1.63-v1.66.
- Gameplay не трогался; `_core.js`, `frontier.html`, `_harness.js` не менялись.

## Что изменилось в v1.66

- Категории стройки переехали из длинной нижней панели в боковую панель `Архитектор`.
- Нижняя панель оставлена только для быстрых действий: отмена и снос.
- Это HTML/CSS-only срез: IDs кнопок сохранены, игровая логика не менялась.
- Нужна визуальная проверка в браузере: панель не должна перекрывать важные overlay/карту.

## Что изменилось в v1.65

- Полы теперь дают бонус уюта комнате при покрытии >=60%.
- Room-панель показывает строку пола: материал, процент покрытия, статус.
- Scenario BA закрывает голую/застеленную комнату.

## Что изменилось в v1.64

- Добавлены material walls: деревянная и каменная стена.
- Стены блокируют путь, считаются стенами комнаты и имеют разную прочность.
- Scenario AZ закрывает блокировку, room-wall и прочность.

## Что изменилось в v1.63

- Добавлены деревянный и каменный пол как отдельный tile-layer.
- Полы строятся кистью через floor-blueprint, не блокируют мебель, сохраняются в save/load.
- Scenario AY закрывает placement/build/demolish.

## Что изменилось в v1.61

- PC stats/наука теперь показывает компактную herd/animal-панель: табун, приручение, конюшни/ранчо, бонус скорости и доход ранчо.
- `herdDetailRows()` собирает данные панели отдельно от DOM, чтобы следующий агент мог безопасно расширять animal UI.
- Scenario AX проверяет no-stable feedback и активные строки конюшни/ранчо; полный harness A-AX (50) зелёный.

## Что изменилось в v1.60

- PC stats/наука теперь показывает компактную room-панель: каждая найденная комната имеет тип, уют, качество стен, размер и мебель.
- `roomDetailRows()` собирает данные панели отдельно от DOM, чтобы следующий агент мог безопасно расширять room UI.
- Scenario AW проверяет две комнаты и readable rows; полный harness A-AW (49) зелёный.

## Что изменилось в v1.59

- Комнаты теперь показывают качество стен/формы: `тесная защита`, `крепкие стены`, `широкая комната`.
- Stats и overlay мебели показывают wall quality вместе с типом комнаты.
- Scenario AV проверяет компактную и просторную комнаты, summary и label.

## Что изменилось в v1.58

- По просьбе пользователя mobile-направление отложено примерно на 4 недели; текущий фокус снова PC-first.
- Закрытые комнаты теперь получают тип: спальня, столовая, жилая комната или украшенный угол.
- Stats показывает summary типов комнат, а overlay кровати/стола/декора показывает тип конкретной комнаты и score.
- `enclosedRoomAt()` возвращает клетки комнаты, чтобы группировать мебель внутри одной комнаты.
- Scenario AU проверяет спальню/столовую/жилую комнату и fallback для открытого места.

## Что изменилось в v1.57

- Добавлен mobile-only drawer для телефонов: вкладки `Пешки` и `Лог` доступны без desktop sidebar.
- Drawer стартует свернутым, открывается кнопкой `👥`, переключает вкладки и переиспользует те же карточки пешек, что desktop.
- На 390px drawer остаётся слева от миникарты 96px и не создаёт horizontal overflow.
- Local mobile browser smoke: build 1.57, 3 pawn cards, log entries, no minimap overlap, console errors = 0.

## Что изменилось в v1.56

- Принят и завершён PAUSED-срез Claude по ремонту построек.
- Строители теперь чинят повреждённые готовые здания, если нет blueprint-строек.
- `tryRepair()` резервирует повреждённое здание, ведёт строителя к нему, восстанавливает HP и логирует завершение.
- Это закрывает loop после поджигателя/урона по обороне: стены и постройки можно восстановить без ручного пересоздания.
- Scenario AT проверяет ремонт повреждённого забора до полного HP и no-op, когда чинить нечего.

## Что изменилось в v1.44

- Добавлен первый room-recognition: мебель внутри небольшой замкнутой зоны из забора/ворот считается комнатой.
- Room comfort даёт небольшой mood-бонус и мысль "Хорошая комната".
- Stats-панель показывает `Комнаты: <label> (score/3)`.
- Info-overlay кровати/стола/декора показывает, закрыта ли мебель стенами.
- Scenario AI проверяет открытую мебель против закрытой комнаты: score, bonus, label и thought.

## Что изменилось в v1.43

- По скрину из `fix/` исправлен UX нижней стройки: кнопки больше не лежат одной большой простынёй.
- Нижняя панель строительства сгруппирована в папки: еда, логистика, оборона, город, ранчо, дом.
- Папка открывается вверх, после выбора постройки закрывается, активная категория подсвечивается.
- Миникарта и zoom-controls используют измеренную высоту bottom bar, поэтому не перекрывают строительство на desktop/mobile.
- Browser smoke: desktop 1600x900 и mobile 390x844, без horizontal overflow, без console errors, миникарта выше bottom bar.

## Что изменилось в v1.42

- Добавлено состояние табуна `G.herd`: дикие лошади, приручённые лошади, прогресс приручения.
- Готовые конюшни каждый день приручают диких лошадей; ранчо немного помогает скорости приручения.
- Приручённые лошади дают небольшой бонус скорости и усиливают дневной доход ранчо.
- Info-overlay конюшни/ранчо и stats-панель показывают состояние лошадей.
- Save/load сохраняет `herd`; старые сейвы нормализуются через `normalizeGameState()`.
- Scenario AH проверяет no-stable/no-taming, приручение, бонус ранчо и бонус скорости.

## Что изменилось в v1.41

- Добавлена постройка `ranch` / "Ранчо" как следующий срез лошадиного трека.
- Ранчо даёт дневной yield еды и золота только если есть готовая конюшня.
- Добавлены кнопка строительства, отрисовка ранчо и строка info-overlay по дневной выдаче.
- Scenario AG проверяет no-yield без конюшни и food/gold yield с конюшней.

## Что изменилось в v1.40

- Добавлен общий счёт `Комфорт усадьбы` из готовых кровати, стола и декора.
- Полный набор мебели даёт небольшой общий mood delta bonus.
- В мыслях появляется "Уютная усадьба".
- Stats-панель показывает `Комфорт усадьбы: <label> (score/3)`.
- Scenario AF проверяет score 0→3, bonus, label и мысль.

## Что изменилось в v1.39

- Добавлена постройка `decor` / "Декор" как первый beauty-срез.
- Если ковбой рядом с готовым декором, его mood delta немного растёт.
- Добавлена мысль "Красивый уголок", отрисовка декора, кнопка строительства и info-overlay строка.
- Scenario AE проверяет nearby beauty bonus и мысль.

## Что изменилось в v1.38

- Добавлена постройка `table` / "Стол" как второй маленький срез мебели.
- Если в колонии есть готовый стол, при еде ковбой получает небольшой mood-бонус.
- Добавлена мысль "Ел за столом", отрисовка стола, кнопка строительства и info-overlay строка.
- Scenario AD проверяет бонус еды за столом против еды без стола.

## Что изменилось в v1.37

- Добавлена постройка `bed` / "Кровать" как первый маленький срез мебели.
- Ковбой во время сна выбирает свободную кровать раньше лагеря.
- Сон рядом с кроватью быстрее восстанавливает энергию и даёт небольшой бонус настроения.
- Добавлены кнопка строительства, отрисовка кровати и строка info-overlay по комфорту.
- Scenario AC проверяет бонус сна и выбор кровати вместо лагеря.

## Что изменилось в v1.36

- Публичный `index.html` переделан из технической страницы в понятный русский статус проекта.
- На сайте теперь видно: что уже сделано, что будет дальше, план по неделям, ссылки на документы.
- Старые кнопки/лейблы `Roadmap.md` и `Phone access` убраны из первого экрана.
- Добавлен визуальный preview игры: `assets/site-game-preview.png`.
- Мобильная игра: миникарта уменьшена до 96x72 и поднята над нижней панелью.
- Мобильная игра: zoom-кнопки и keyboard hints скрыты, sidebar остаётся скрытым.
- Локальный smoke 390x844: сайт без horizontal overflow, игра без horizontal overflow, sidebar hidden, console errors = 0.

## Следующая задача по roadmap

Брать маленький проверяемый срез из Недели 2, не занимая чужой `IN PROGRESS`:

1. Phase 3: плавание и вода как препятствие (скорость/риск от навыка, груза и погоды).
2. Phase 3/4: события POI — руины дают находки, лагеря бандитов давление, прииски золото.
3. Frontier-контент: загоны/скот, салун глубже, караванные риски.
4. Каждый gameplay-срез закрыть новым scenario в `_harness.js`.

## Координация Codex + Cloud

- Перед стартом читать `docs/WIP.md`.
- Если задача уже `IN PROGRESS` у другого агента, брать другую.
- При старте вписывать свою задачу в `docs/WIP.md`.
- После завершения и commit убирать задачу из WIP.
- Если работа оборвалась, оставить `PAUSED` с файлами, что сделано и что осталось.

## Критичные правила сборки

- Если менялся `_core.js`, запускать только `node build.js`.
- Не синхронизировать `frontier.html` вручную через PowerShell `Get-Content -Raw` / `Set-Content`.
- Перед push: `node _harness.js`.
- После push проверить:
  - https://osminoog09-star.github.io/frontier/
  - https://osminoog09-star.github.io/frontier/frontier.html

## Важные файлы

- `_core.js` - игровая логика.
- `frontier.template.html` - HTML/CSS оболочка.
- `frontier.html` - собранная публичная игра.
- `index.html` - публичный сайт/status.
- `_harness.js` - автотесты и guards.
- `ROADMAP.md` - понятный roadmap.
- `docs/TECH_ROADMAP.md` - технический roadmap.
- `docs/CHANGELOG.md` - история срезов.
- `docs/WIP.md` - замок задач.
- `AGENTS.md` - правила агентов.
- `fix/` - входящие скриншоты от пользователя; периодически проверять и разбирать как UX/bug feedback.
